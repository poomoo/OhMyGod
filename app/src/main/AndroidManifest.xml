<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.poomoo.ohmygod">

    <!-- 这个权限用于进行网络定位-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 这个权限用于访问GPS定位-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位-->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 获取运营商信息，用于支持提供运营商信息相关的接口-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 这个权限用于获取wifi的获取权限，wifi信息会用来进行网络定位-->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- 用于读取手机当前的状态-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 访问网络，网络定位需要上网-->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SD卡读取权限，用户写入离线定位数据-->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:name=".application.MyApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.activity.TestActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.LogInActivity" />
        <activity android:name=".view.activity.MainFragmentActivity" />
        <activity android:name=".view.activity.WebViewActivity" />
        <activity android:name=".view.activity.SnatchRecordActivity" />
        <activity android:name=".view.activity.WinningRecordActivity" />
        <activity android:name=".view.activity.WinningRecord2Activity" />
        <activity android:name=".view.activity.ShowAndShareActivity" />
        <activity android:name=".view.activity.MyWithdrawDepositActivity" />
        <activity android:name=".view.activity.WithdrawDepositActivity" />
        <activity android:name=".view.activity.VerifyPhoneNumActivity" />
        <activity android:name=".view.activity.WithdrawDepositDetailsActivity" />
        <activity android:name=".view.activity.InStationMessagesActivity" />
        <activity android:name=".view.activity.MyShowActivity" />
        <activity android:name=".view.activity.SettingActivity" />
        <activity android:name=".view.activity.UserInfoActivity" />
        <activity android:name=".view.activity.VerifyPhoneNum2Activity" />
        <activity android:name=".view.activity.ChangePhone2Activity" />
        <activity android:name=".view.activity.ChangePhone3Activity" />
        <activity android:name=".view.activity.ChangeIdCardInfoActivity" />
        <activity android:name=".view.activity.IdCardInfoActivity" />
        <activity android:name=".view.activity.BankCardActivity" />
        <activity android:name=".view.activity.ChangeBankCardNumActivity" />
        <activity android:name=".view.activity.ChangeBankCardInfoActivity" />
        <activity android:name=".view.activity.ResetPassWordActivity" />
        <activity android:name=".view.activity.RegisterActivity" />
        <activity android:name=".view.activity.SelectActivity" />
        <activity android:name=".view.activity.CommodityInformationActivity" />
        <activity android:name=".view.activity.EditPersonalInformationActivity" />
        <activity android:name=".view.activity.NickNameActivity" />
        <activity android:name=".view.activity.FeedBackActivity" />
        <activity android:name=".view.activity.CityListActivity" />
        <activity android:name=".view.activity.WinInformationActivity" />

        <activity android:name=".view.activity.pics.ImageGridActivity" />
        <activity android:name=".view.activity.pics.PhotosActivity" />
        <activity android:name=".view.activity.pics.PhotoActivity" />

        <service
            android:name=".service.Get_UserInfo_Service"
            android:exported="false" />

        <service
            android:name="com.baidu.location.f"
            android:enabled="true"
            android:process=":remote">
            <intent-filter>
                <action android:name="com.baidu.location.service_v2.2" />
            </intent-filter>
        </service>

        <!-- meta-data需要写在application中 -->
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="GjKxyYWS4udq2jmqIuCvMPLy" />

        <!-- ############ QQ空间和QQ SSO授权的Activity注册 ############ -->
        <activity
            android:name="com.tencent.tauth.AuthActivity"
            android:launchMode="singleTask"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- 100424468，如果你使用的公用账号，则不需要修改；否则修改成你在QQ开放平台申请的 APP ID -->
                <data android:scheme="tencent100424468" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.tencent.connect.common.AssistActivity"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"></activity>
        <!-- meta-data需要写在application中 -->
        <meta-data
            android:name="UMENG_APPKEY"
            android:value="566f853a67e58e3423000e78" />

        <activity
            android:name="com.umeng.socialize.view.ShareActivity"
            android:configChanges="orientation|keyboard"
            android:exported="false"
            android:launchMode="singleTask"
            android:noHistory="true"
            android:theme="@style/Theme.UMDialog"
            android:windowSoftInputMode="stateVisible|adjustResize" />
    </application>
</manifest>
